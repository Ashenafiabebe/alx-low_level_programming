#include "main.h"

/**
 * _isupper - check if the letter is cappital
 * @x: The number to be checked
 * Return: Always 0.
 */

int main(void)

{
	if (x >= 65 && x <= 98)
	{
	return (1);
	}
	return (0);
}
